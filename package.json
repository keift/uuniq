{
  "name": "uuniq",
  "version": "1.2.0",
  "description": "Unique Snowflake IDs and Symbolic IDs that can be generated for thousands of years.",
  "license": "MIT",
  "scripts": {
    "dev:snowflake": "nodemon ./test/test_snowflake.js",
    "dev:symbolic": "nodemon ./test/test_symbolic.js",
    "build": "tsup ./main.ts --dts --clean --external=any-base --format esm,cjs --out-dir ./dist"
  },
  "dependencies": {
    "any-base": "^1.1.0"
  },
  "devDependencies": {
    "tsup": "^8.4.0",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">= 14.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/keift/uuniq.git"
  },
  "keywords": [
    "id",
    "unique",
    "unique-id",
    "snowflake",
    "snowflake-id",
    "symbolic",
    "symbolic-id"
  ]
}